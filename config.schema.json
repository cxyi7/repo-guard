{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "repo-guard project configuration",
  "description": "Project-level protected-file, staged quality, TypeScript, build, unit-test, notification, and Vue Lighthouse rules consumed by @cxyi7/repo-guard.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "version",
    "rules"
  ],
  "properties": {
    "$schema": {
      "type": "string"
    },
    "version": {
      "const": 1,
      "description": "Configuration format version."
    },
    "notification": {
      "type": "object",
      "description": "Project-wide WeCom notification switch. Missing values stay enabled for compatibility.",
      "additionalProperties": false,
      "properties": {
        "enabled": {
          "type": "boolean",
          "default": true,
          "description": "Require WeCom delivery for notify-level rules. When false, protected changes are still recorded but no notification is sent."
        }
      }
    },
    "build": {
      "type": "object",
      "description": "Project-owned production build script run explicitly or from the managed pre-push hook.",
      "additionalProperties": false,
      "properties": {
        "enabled": {
          "type": "boolean",
          "default": false,
          "description": "Run the configured production build automatically from the managed pre-push hook. Init enables this only when the script is already available."
        },
        "script": {
          "type": "string",
          "minLength": 1,
          "pattern": "^[A-Za-z0-9:_-]+$",
          "default": "build",
          "description": "Name of the consuming project's npm production-build script."
        },
        "timeoutMs": {
          "type": "integer",
          "minimum": 1,
          "default": 300000,
          "description": "Maximum runtime for the project build process."
        }
      }
    },
    "lighthouse": {
      "type": "object",
      "description": "Vue Lighthouse CI runner. The consuming project owns @lhci/cli, Chrome, routes, assertions, and lighthouserc configuration.",
      "additionalProperties": false,
      "properties": {
        "enabled": {
          "type": "boolean",
          "default": false,
          "description": "Run the Vue Lighthouse gate automatically from the managed pre-push hook. The explicit repo-guard lighthouse command can run while this is false."
        },
        "configFile": {
          "type": [
            "string",
            "null"
          ],
          "minLength": 1,
          "default": null,
          "description": "Repository-relative Lighthouse CI config file. Null detects a standard lighthouserc filename."
        },
        "buildScript": {
          "type": [
            "string",
            "null"
          ],
          "minLength": 1,
          "default": "build",
          "description": "Project npm script run before Lighthouse collection. Null skips the build step."
        },
        "timeoutMs": {
          "type": "integer",
          "minimum": 1,
          "default": 300000,
          "description": "Timeout applied separately to the build, collect, and assert processes."
        }
      }
    },
    "typeCheck": {
      "type": "object",
      "description": "Project-owned TypeScript type-check script run explicitly or from the managed pre-push hook.",
      "additionalProperties": false,
      "properties": {
        "enabled": {
          "type": "boolean",
          "default": false,
          "description": "Run the configured TypeScript type-check script automatically from the managed pre-push hook. Init enables this only when the script is already available."
        },
        "script": {
          "type": "string",
          "minLength": 1,
          "pattern": "^[A-Za-z0-9:_-]+$",
          "default": "typecheck",
          "description": "Name of the consuming project's npm script that runs tsc, vue-tsc, or an equivalent full-project type check."
        },
        "timeoutMs": {
          "type": "integer",
          "minimum": 1,
          "default": 180000,
          "description": "Maximum runtime for the TypeScript type-check process."
        }
      }
    },
    "unitTest": {
      "type": "object",
      "description": "JavaScript/Vue unit-test policy and pre-push Vitest gate. The consuming project owns Vitest, Vue Test Utils, its test environment, configuration, and tests.",
      "additionalProperties": false,
      "properties": {
        "enabled": {
          "type": "boolean",
          "default": false,
          "description": "Run missing-test and bypass checks followed by the configured unit-test script from the managed pre-push hook. Init enables this only when Vitest and the script are already available."
        },
        "script": {
          "type": "string",
          "minLength": 1,
          "default": "test:unit",
          "description": "Name of the consuming project's npm script used to run unit tests."
        },
        "timeoutMs": {
          "type": "integer",
          "minimum": 1,
          "default": 120000,
          "description": "Maximum runtime for the unit-test process."
        },
        "coverage": {
          "type": "boolean",
          "default": false,
          "description": "Append --coverage to the configured npm script. The project must provide a compatible Vitest coverage provider and thresholds."
        },
        "requireTests": {
          "type": "string",
          "enum": [
            "newFiles",
            "changedFiles"
          ],
          "default": "newFiles",
          "description": "Require colocated tests only for newly added/copied source files, or for every changed source file."
        },
        "sourcePatterns": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "string",
            "minLength": 1
          },
          "default": [
            "src/utils/**/*.js",
            "src/composables/**/*.js",
            "src/stores/**/*.js",
            "src/api/**/*.js",
            "src/components/**/*.vue"
          ],
          "description": "Repository-relative .js, .mjs, .cjs, .jsx, and .vue source globs that require a colocated .spec.js file."
        },
        "testPatterns": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "string",
            "minLength": 1
          },
          "default": [
            "**/*.spec.js"
          ],
          "description": "Repository-relative test globs scanned for empty tests and forbidden .skip, .skipIf, .todo, and .only calls while ignoring comments and literals."
        },
        "exclusions": {
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1
          },
          "default": [
            "src/main.js",
            "src/**/index.js",
            "src/generated/**"
          ],
          "description": "Source paths excluded from the missing-test requirement, such as entry points and generated files."
        }
      }
    },
    "preCommit": {
      "type": "object",
      "description": "Staged quality gates. Missing defaults can be materialized with repo-guard migrate.",
      "additionalProperties": false,
      "properties": {
        "filePlacement": {
          "type": "object",
          "description": "Configurable staged and full-project file location policy.",
          "additionalProperties": false,
          "properties": {
            "enabled": {
              "type": "boolean",
              "default": true,
              "description": "Automatically enforce file placement in pre-commit. Explicit full-project checks still use the configured rules when this switch is false."
            },
            "mode": {
              "type": "string",
              "enum": [
                "newFiles",
                "changedFiles"
              ],
              "default": "newFiles",
              "description": "newFiles checks added, copied, and renamed paths; changedFiles checks every non-deleted staged matching file."
            },
            "rules": {
              "type": "array",
              "minItems": 1,
              "default": [
                {
                  "name": "资源文件",
                  "patterns": [
                    "**/*.{png,jpg,jpeg,gif,webp,avif,svg,ico,bmp,tif,tiff}",
                    "**/*.{woff,woff2,ttf,otf,eot}",
                    "**/*.{mp3,wav,ogg,m4a,mp4,webm,mov,pdf}"
                  ],
                  "allowedPatterns": [
                    "src/assets/**",
                    "public/assets/**",
                    "docs/assets/**"
                  ],
                  "exceptions": [
                    "public/favicon.{ico,png,svg}"
                  ],
                  "suggestedDirectory": "src/assets"
                },
                {
                  "name": "Markdown 文档",
                  "patterns": [
                    "**/*.md"
                  ],
                  "allowedPatterns": [
                    "docs/**",
                    ".github/**",
                    ".changeset/**"
                  ],
                  "exceptions": [
                    "README*.md",
                    "CHANGELOG*.md",
                    "AGENTS.md",
                    "SECURITY.md",
                    "CONTRIBUTING.md",
                    "CODE_OF_CONDUCT.md",
                    "LICENSE*.md"
                  ],
                  "suggestedDirectory": "docs"
                }
              ],
              "items": {
                "type": "object",
                "additionalProperties": false,
                "required": [
                  "name",
                  "patterns",
                  "allowedPatterns",
                  "suggestedDirectory"
                ],
                "properties": {
                  "name": {
                    "type": "string",
                    "minLength": 1,
                    "description": "Human-readable file category used in AI repair instructions."
                  },
                  "patterns": {
                    "type": "array",
                    "minItems": 1,
                    "items": {
                      "type": "string",
                      "minLength": 1
                    },
                    "description": "Case-insensitive repository-relative globs selecting files for this rule. The first matching rule wins."
                  },
                  "allowedPatterns": {
                    "type": "array",
                    "minItems": 1,
                    "items": {
                      "type": "string",
                      "minLength": 1
                    },
                    "description": "Repository-relative locations allowed for matching files."
                  },
                  "exceptions": {
                    "type": "array",
                    "default": [],
                    "items": {
                      "type": "string",
                      "minLength": 1
                    },
                    "description": "Special matching files exempt from this rule."
                  },
                  "suggestedDirectory": {
                    "type": "string",
                    "minLength": 1,
                    "description": "Concrete repository-relative directory proposed to AI when a file is misplaced."
                  }
                }
              }
            }
          }
        },
        "maxFileLines": {
          "type": "object",
          "description": "Maximum physical line counts for complete staged files, evaluated after formatting and lint verification.",
          "additionalProperties": false,
          "properties": {
            "enabled": {
              "type": "boolean",
              "default": false,
              "description": "Reject staged files that exceed the first matching rule and print standalone AI refactoring instructions. New init configurations set this to true; omitted values stay false for upgrade compatibility."
            },
            "mode": {
              "type": "string",
              "enum": [
                "strict",
                "noRegression"
              ],
              "default": "strict",
              "description": "strict always rejects an over-limit file; noRegression allows an existing over-limit file only while its staged line count does not exceed its HEAD baseline. New files remain strict."
            },
            "warnAt": {
              "type": "number",
              "exclusiveMinimum": 0,
              "maximum": 1,
              "default": 0.85,
              "description": "Non-blocking warning threshold expressed as a fraction of maxLines."
            },
            "rules": {
              "type": "array",
              "minItems": 1,
              "default": [
                {
                  "pattern": "**/*.vue",
                  "maxLines": 700
                },
                {
                  "pattern": "**/*.{js,mjs,cjs,jsx}",
                  "maxLines": 1000
                },
                {
                  "pattern": "**/*.{ts,tsx}",
                  "maxLines": 1000
                }
              ],
              "items": {
                "type": "object",
                "additionalProperties": false,
                "required": [
                  "pattern",
                  "maxLines"
                ],
                "properties": {
                  "pattern": {
                    "type": "string",
                    "minLength": 1,
                    "description": "Repository-relative glob matched against staged file paths."
                  },
                  "maxLines": {
                    "type": "integer",
                    "minimum": 1,
                    "description": "Maximum allowed physical line count, including comments and blank lines."
                  }
                }
              }
            },
            "exclusions": {
              "type": "array",
              "default": [],
              "description": "Repository-relative globs excluded before line-limit rules are matched.",
              "items": {
                "type": "string",
                "minLength": 1
              }
            }
          }
        },
        "stylelint": {
          "type": "object",
          "description": "Stylelint gate loaded from the consuming project's installation and configuration.",
          "additionalProperties": false,
          "properties": {
            "enabled": {
              "type": "boolean",
              "default": false,
              "description": "Run the consuming project's Stylelint against staged style files before the protected-file gate."
            },
            "pattern": {
              "type": "string",
              "minLength": 1,
              "default": "**/*.{css,scss,sass,less,vue}",
              "description": "Glob used to select staged files for Stylelint."
            },
            "fix": {
              "type": "boolean",
              "default": true,
              "description": "Apply Stylelint auto-fixes before the final verification pass."
            },
            "maxWarnings": {
              "type": "integer",
              "minimum": 0,
              "default": 0,
              "description": "Maximum Stylelint warning count allowed by the commit gate."
            },
            "requireConfig": {
              "type": "boolean",
              "default": true,
              "description": "Require a project Stylelint configuration when the staged gate is enabled."
            }
          }
        },
        "prettier": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "enabled": {
              "type": "boolean",
              "default": false,
              "description": "Run Prettier against staged files before the protected-file gate. New init configurations set this to true; omitted values stay false for upgrade compatibility."
            },
            "pattern": {
              "type": "string",
              "minLength": 1,
              "default": "*.{js,jsx,mjs,cjs,ts,tsx,vue,json,json5,jsonc,css,scss,less,html,md,mdx,yml,yaml}",
              "description": "Glob used to select staged files for Prettier."
            },
            "fix": {
              "type": "boolean",
              "default": true,
              "description": "Format staged files automatically; false performs a check-only gate."
            },
            "requireConfig": {
              "type": "boolean",
              "default": true,
              "description": "Require a project Prettier configuration for every matched file."
            }
          }
        },
        "eslint": {
          "type": "object",
          "description": "ESLint is loaded from the consuming project. The optional repo-guard preset is injected as a base Flat Config, then the project's own ESLint configuration is loaded and may override it.",
          "additionalProperties": false,
          "properties": {
            "enabled": {
              "type": "boolean",
              "default": false,
              "description": "Run ESLint against staged files before the protected-file gate. New init configurations set this to true; omitted values stay false for upgrade compatibility."
            },
            "preset": {
              "type": "boolean",
              "default": false,
              "description": "Automatically inject the repo-guard AI maintainability preset before the project's ESLint Flat Config. New init configurations set this to true; omitted values stay false for upgrade compatibility."
            },
            "pattern": {
              "type": "string",
              "minLength": 1,
              "default": "*.{js,jsx,ts,tsx,vue}",
              "description": "lint-staged glob used to select staged files."
            },
            "fix": {
              "type": "boolean",
              "default": true,
              "description": "Apply ESLint auto-fixes before the final verification pass."
            },
            "maxWarnings": {
              "type": "integer",
              "minimum": 0,
              "default": 0,
              "description": "Maximum ESLint warning count allowed by the commit gate."
            }
          }
        }
      }
    },
    "rules": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "pattern",
          "category",
          "level"
        ],
        "properties": {
          "pattern": {
            "type": "string",
            "minLength": 1,
            "description": "Repository-relative glob. * matches one path segment and ** crosses directories."
          },
          "category": {
            "type": "string",
            "minLength": 1,
            "description": "Human-readable category included in reports and notifications."
          },
          "level": {
            "type": "string",
            "enum": [
              "notify",
              "audit"
            ],
            "description": "notify requires successful delivery when notification.enabled is true; audit only records the change."
          }
        }
      }
    },
    "exclusions": {
      "type": "array",
      "uniqueItems": true,
      "items": {
        "type": "string",
        "minLength": 1
      },
      "default": []
    }
  }
}

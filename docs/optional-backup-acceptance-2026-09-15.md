# 可选备份蓝绿部署验收（2026-09-15）

## 本次安装产物

- 包版本：2.1.0（未发布）。
- 源码基线：`d0ea6fe00f668535c27b8090a2adaa87b20951b2`，工作分支 `feat/blue-green-deployment-2.1.0`；打包包含当前未提交的蓝绿部署、通知修复和可选备份代码，不代表该基线提交本身已经包含这些能力。
- 安装包 SHA-256：`b3e502879b130f9e3ff97f58e725df062564787416163274245cf8bb83189808`。
- 两个消费项目均通过本次 `.vendor/cxyi7-repo-guard-2.1.0-optional-backup.tgz` 安装并更新锁文件，已核对运行时文件与包仓库一致。没有使用源码链接或公共 npm 旧包。
- 打包清单 551 个文件，已核对运行时、Schema、文档齐全，不含测试临时目录与本地技能。此验收记录和最终变更日志补充在打包后写入，不改变已验收运行时代码。

## 配置与行为

`blueGreen.backup.enabled` 默认 `false`；不配置备份时，MySQL、Redis 连接由应用环境变量决定，上传目录仍然挂载。备份资源归属检查、数据快照和数据恢复均不执行。普通发布期间旧应用继续服务，候选及正式入口 `/api/health` 通过后才停止并保留旧应用。

显式启用备份后必须声明至少一种数据资源；仍执行维护、发布前快照及恢复。开启备份时若上一版本无恢复点，则在切换前阻断；关闭备份后可仅回退应用。中断恢复遵循待恢复记录，不会因随后关闭开关而跳过已登记的数据恢复。

## 包内检查

- `npm run check`：通过（格式规则、架构、语法、中文文案）。
- 蓝绿配置、状态机、数据保留、上传挂载、通知、恢复和退出码相关测试：44 项通过。
- 全量 `npm test`：1572 项，1548 通过、16 跳过、8 失败；失败集中在 Windows 子进程清理、目录占用和超时。
- 相关失败文件串行复测：84 项，81 通过、2 跳过、1 失败。剩余为 `test/gates/testing/external-gate.test.js` 的 npm 进程树超时终止场景；不得描述为全量零失败。
- `npm run pack:check` 与实际打包：通过。

## 消费项目

本机实际目录为 `D:/Projects/repo-guard-acceptance` 和 `D:/Projects/repo-guard-acceptance-back`。两者使用 `feat/optional-backup-0.2.0`，未合入 main。

| 项目 | 本次提交 | 流水线 |
| --- | --- | --- |
| Vue 前端 | `ca3b66a670faf760a5105d44b7f459ca9ae5b276` | [3344](http://47.120.4.95:7188/xgjy/repo-guard-acceptance/-/pipelines/3344) |
| Java 后端 | `b9a83620e3dec762d5d5a1571951d6aeb1900a76` | [3345](http://47.120.4.95:7188/xgjy/repo-guard-acceptance-back/-/pipelines/3345) |

前端本机类型检查、构建、4 项 Vitest 测试通过。后端默认 Java 17 无法编译项目的 Java 21 目标；改用本机现有 JDK 25 按 release 21 构建及测试通过，GitLab 使用专用 Java 21 Runner。各自不适用的另一技术栈检查保持不适用，未计为通过。

首次新分支推送的流水线 3342、3343 缺少有效 `CI_COMMIT_BEFORE_SHA`，质量门禁拒绝不可信范围并阻断部署，飞书确认发送失败通知。提交接入说明后建立真实前后提交范围，重新触发上表流水线；没有关闭门禁或伪造基准版本。

## 真实部署与数据验收

两条流水线的质量、构建和部署作业均成功，实际日志明确记录“本次不备份或恢复数据”，部署源码分别为上表提交。前端 Vitest 4 项通过，后端 Java 21 下 5 项测试通过。两条流水线都收到飞书对开始和成功通知的确认。

服务器独立验收副本通过本机提交的 Git bundle 建立，并安装同一产物；没有向服务器添加 GitLab 密码或令牌。

后端实际验证：

- 新版本写入专用 MySQL 任务、上传文件和 Redis 测试键，然后通过真实 `ops rollback` 回退，再次 `ops rollback` 切回；两次操作后三类数据均保持不变。
- 在候选容器设置错误监听端口，真实健康探针失败，包运行时自动恢复旧应用；旧应用健康、三类数据仍保留，整个无备份阶段没有新增快照目录，飞书确认开始和失败回退通知。
- 使用同一安装包与已验收镜像，临时启用备份执行运行时回归：创建三类快照，删除本轮测试数据，再回滚恢复，三类数据均恢复成功。该回归使用真实 Docker 和 MySQL／Redis／目录，没有模拟数据适配器；消费项目保存的配置仍为备份关闭。
- 最终按本轮任务 ID、文件 ID、Redis 键清理测试数据，未修改 community 或其他业务资源。

旧应用也不健康、恢复失败保持维护，以及中断后改变配置的恢复语义由包内状态机测试覆盖；本次未故意破坏真实数据库或旧应用。企业微信发送使用自动化测试覆盖，当前真实配置只有飞书，未声称真实企业微信发送通过。

前端也通过真实 `ops rollback` 回退及再次切换，最后恢复本次 `ca3b66a` 版本；没有通过前端进行额外业务写入。公网 `18080/api/health` 返回 200 且入口版本匹配；后端在服务器内网 18081 完成健康与数据验收。从本机公网直连 18081 超时，不作为公网可用通过证据；现有站点通过 18080 代理访问后端。

证据：本机 `test/.tmp/optional-backup-*.log`；服务器 `/srv/repo-guard-acceptance/build/optional-backup-smoke.log`、`optional-backup-rollback.log`、`optional-backup-forward.log`。测试日志不进入 npm 安装产物。

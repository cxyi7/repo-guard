# 首次分支推送与蓝绿部署交付收尾验收（2026-09-15）

## 安装产物与源码

- 包版本：`2.1.0`，未发布到 npm。
- 源码提交：`284be8b42f3ab6f92054aabf0f12377cc78bbab1`，打包时工作区干净。
- 产物：`cxyi7-repo-guard-2.1.0-first-push.tgz`。
- SHA-256：`b45fefdc1288bf3f4e844cbdcdc9367007b682e7e19eac9b1d17d1fd54449f80`。
- 两个固定项目均通过本地产物安装并提交依赖与锁文件；552 个安装文件逐一与源码核对一致，无源码链接。打包清单不含临时测试目录、本地技能或凭据文件。
- 本记录及待办完成登记在打包后补充，不改变已验收的运行时代码。

## 包内验证

- `npm ci`、`npm run check`、`npm run pack:check` 和实际打包通过。
- 首次推送与 CI 生命周期专项：8 项通过。
- 真实子进程超时与外部门禁专项：24 项通过。
- 最终完整 `npm test`：1577 项，1561 通过、16 跳过、0 失败。跳过项未计为通过。
- Windows 集成测试在具备真实进程终止权限的环境执行；此前受限环境中的失败不作为本次通过证据。测试仍核对实际父子进程退出及无延迟遗留文件写入，清理失败仍报告执行错误并保留超时或取消原因。
- 仓库测试默认并发为 4；允许显式覆盖，不减少测试发现范围。

## 两个消费项目

两者使用专用 `test/first-push-0.2.0` 分支；只运行质量检查和构建，不包含部署作业。

| 项目 | 本次提交 | 流水线 |
| --- | --- | --- |
| Vue 前端 | `3cd97e4b60bc8a6e67f07279fb4da7f7ced83acb` | [3346](http://47.120.4.95:7188/xgjy/repo-guard-acceptance/-/pipelines/3346) |
| Java 后端 | `27e77460307bb8836aaf6bc3a4a6d1e06db91bb8` | [3347](http://47.120.4.95:7188/xgjy/repo-guard-acceptance-back/-/pipelines/3347) |

本机安装及原有提交、推送 Hook 均通过，保留通知型配置保护检查的证据。前端类型检查、4 项 Vitest 测试、构建通过；后端使用现有 JDK 25 按项目 release 21 编译，5 项 JUnit 测试与构建通过。前后端另一技术栈的检查不适用，未计为通过。

两条远程流水线的质量检查与构建作业全部成功。专用断言确认真实 `CI_COMMIT_BEFORE_SHA` 为全零、来源为 push、历史完整，报告 base 等于当前提交与获取到的默认分支唯一共同祖先，报告 head 等于本次提交且状态通过。没有通过追加第二次提交建立前序 SHA 来绕过首次推送问题。

| 项目 | 默认分支 | 报告基准 | 检查／构建作业 |
| --- | --- | --- | --- |
| 前端 | `main` | `87a4d292653177470c28cb558712b3a0ce0f151b` | `7911`／`7912`，均成功 |
| 后端 | `main` | `9b471ffcc9a7b722a899ab86ef7c7c0658e69e9b` | `7913`／`7914`，均成功 |

两者在 detached HEAD 下均通过通知分支断言，显示 `test/first-push-0.2.0`，且飞书确认真实发送成功。后端 Runner 使用 Java 21；两条构建作业在检查通过后才执行，流水线没有部署作业。

## 部署证据边界

实现提交 `284be8b` 与验收记录提交 `a9b6912` 已快进合入并推送 `refactor/node-engineering-2.0.0`，随后仅补充完成登记。没有发布 npm 包。

此前前后端实际部署、手动回退、可选快照恢复和飞书通知见[可选备份验收](optional-backup-acceptance-2026-09-15.md)。本轮新增首次推送修复不改变部署状态机；没有重复部署或修改业务数据，也不以本次质量流水线替代真实部署验收。

本轮只读确认专用前后端容器健康，服务器 `18080/api/health` 返回正常及后端原活动提交 `b9a83620e3dec762d5d5a1571951d6aeb1900a76`。没有新增端口。

企业微信真实发送、恢复失败的真实故障演练及其他功能待办仍按[流程待办](end-to-end-workflow-backlog.md)登记，不将本轮收尾解释为这些范围全部完成。

证据保留于本机 `test/.tmp/finish-*.log`，流水线提供对应 CI 报告及首次推送报告；日志不进入 npm 安装产物。

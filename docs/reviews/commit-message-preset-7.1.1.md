# 提交规范预设 7.1.1 审查记录

本轮仅调整新建配置和显式启用时的提交规范预设：enabled=true、merge.allowed=false、breakingChange.requireMajorVersionOnRelease=false。通用读取回退值不变，已有显式值优先。新建配置与显式启用复用 COMMIT_MESSAGE_PRESET，不新增配置结构。

本地自动变更文件摘要沿用现有实现，不新增推送或 CI 的摘要完整性核验。现有提交格式历史复核不变。受控挑拣与接口验收只记录在交付合同后续计划，未实现、未开启。

## 审查与复现

- 真实创建两个分支，执行带普通 feat 标题的非快进 merge，commit-msg 根据多个父节点阻断，HEAD 不变。
- 中止 merge 后创建普通 fix 提交，标题保留，仅追加一次本轮文件摘要，不夹带被中止分支的文件。
- 配置字段缺失时启用会补齐预设；用户显式允许 merge、修改标题长度时保持原值。重复启用不重复改写，关闭不删除其他配置。
- 旧测试夹具将缺失字段自动展开为显式旧值。修正缺失字段测试为直接写入稀疏 JSON；显式旧值保留行为没有被生产代码覆盖。
- 新预设开关的旧断言已更新。无新增依赖、退出码、迁移读取器或受控挑拣入口。

## 验证

最终定向回归 72 项全部通过，覆盖提交门禁、自动摘要、配置管理、AI 规范和文档示例。工程检查和打包检查通过。完整回归 1,503 项：1,483 通过、20 跳过、0 失败；跳过项不作为通过证据。

限制：禁止 merge commit 不能识别 fast-forward、squash 或证明执行了 cherry-pick；摘要由本地 Hook 生成，不承诺绕过 Hook 的提交具有完整摘要。

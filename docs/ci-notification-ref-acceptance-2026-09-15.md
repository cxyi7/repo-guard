# CI 通知分支名修复验收（2026-09-15）

## 修复范围

通知不再只依赖 `git branch --show-current`。GitLab 环境依次读取合并请求源分支、标签、普通分支及引用名称；本地读取实际分支，真正的未附着检出明确显示“指定提交检出（未附着分支）”。所有展示字段继续经过脱敏和单行处理；目标提交与可信范围检查保持原有规则。

本次是未发布 2.1.0 蓝绿部署验收期间发现的通知修复，继续在 `feat/blue-green-deployment-2.1.0` 工作区完成，源码基线 `d0ea6fe00f668535c27b8090a2adaa87b20951b2`。打包包含当前修改及新增运行时代码、配置与文档，尚未提交或发布 npm；本文是验收后补记。

安装产物 `cxyi7-repo-guard-2.1.0.tgz` 的 SHA-256：`C22B8B1027873818C35148E3BE83C61A258EE1B3906B4448A5B4554C3372A32E`。两个消费项目安装同一文件并更新锁文件。

## 包内验证

- 通知、GitLab 通知与错误边界：26 项通过。
- 创建真实 Git 仓库并切至 detached HEAD，验证成功／失败通知、普通分支、MR 源分支、标签、引用回退、本地分支、平台变量缺失和消息脱敏。
- `npm run check` 与 `npm run pack:check` 通过。
- 本次按小范围通知改动执行相关回归，没有重复整个部署功能的完整测试。

## 真实 GitLab 验收

| 项目 | 消费提交 | 流水线 | 结果 |
| --- | --- | --- | --- |
| Vue 前端 | `b2d9284b9e3a2cff5f51587965114bdae1627086` | [3340](http://47.120.4.95:7188/xgjy/repo-guard-acceptance/-/pipelines/3340)，MR 1 | 成功 |
| Java 后端 | `5c8d956b4d6763bf1d45085b95680bdc8cca3454` | [3341](http://47.120.4.95:7188/xgjy/repo-guard-acceptance-back/-/pipelines/3341)，普通分支 | 成功 |

两者使用已有专用分支 `test/ci-acceptance-2.0.0`。流水线仅运行 `verify`，执行项目配置的完整 CI 及通知断言，没有部署作业。前端 MR 的实际 Git 引用为 `refs/merge-requests/1/head`；预期通知展示源分支 `test/ci-acceptance-2.0.0`。

验收脚本首先断言实际 Git 分支为空，然后捕获安装包生成的通知，核对分支行、排除“未命名分支”，最后通过已有飞书机器人真实发送并保存 `reports/ci-notification-ref.json`。作业失败会阻断验收。前端作业 7897、后端作业 7898 均记录三次飞书平台确认（接入测试、CI 结果、分支验收消息），并输出“通知分支验收通过：分支：test/ci-acceptance-2.0.0；实际 Git 检出为 detached HEAD。”；两条流水线都只有 `repo_guard` 作业。平台确认不等同于用户已经阅读。

## 交付边界

修复产物已安装并推送两个 CI 验收分支。部署分支与现有线上容器未变更，旧消息不会自动改写，使用旧安装包的其他分支仍会沿用原显示方式。验收分支采用专用仅检查流水线配置，后续升级部署分支应同步产物和锁文件，不直接覆盖其部署流水线。

企业微信共用相同消息构造逻辑并有自动测试；这次真实发送使用项目已经配置的飞书。公网读写、数据恢复及端口均不属于本次修复范围。

# 蓝绿部署验收记录（2026-09-15）

## 范围与产物

- 包：`@cxyi7/repo-guard@2.1.0`，未发布 npm。
- 源码基线：`d0ea6fe00f668535c27b8090a2adaa87b20951b2`，工作分支 `feat/blue-green-deployment-2.1.0`。
- 安装产物 SHA-256：`7A965D1C6A62790F4F9E348364BD43ABE901B616595D458DFD9C12B935DC898F`。
- 打包范围包含本次工作区修改及新增运行时代码、配置 Schema 和功能文档；当时尚未提交。本文及待办状态是在验收后补记，未包含于上述产物，不把源码基线本身视为已含新功能。
- 实际消费目录为用户指定且存在的 `D:/Projects/repo-guard-acceptance` 与 `D:/Projects/repo-guard-acceptance-back`；规范列出的桌面目录当前不存在。

## 包内验证

锁定安装、代码检查、依赖架构、中文文案检查及打包清单检查通过。部署状态机、通知和错误边界的最终定向回归 25 项通过。

首次完整测试 1562 项：1545 通过、16 跳过、1 项错误边界规范失败。已改用公共错误转换并定向验证，不将该次完整执行写为全通过。最终完整执行 1564 项：1546 通过、16 跳过、2 项 Windows 子进程终止／目录占用失败。随后将这两组测试串行复核，28 项全部通过。完整执行的两项失败仍保留记录，不表述为单次完整零失败；并发进程测试的环境稳定性仍需关注。

## 消费项目与真实流水线

| 项目 | 提交 | 结果 |
| --- | --- | --- |
| Vue 前端 | `ad230bfb31b3203f36ec7b19bae41f70e4306355` | [流水线 3338](http://47.120.4.95:7188/xgjy/repo-guard-acceptance/-/pipelines/3338) 的质量、构建和部署全部成功；部署开始及成功各有飞书确认 |
| Java 后端 | `9b471ffcc9a7b722a899ab86ef7c7c0658e69e9b` | 已合入 `main`；同一提交完整手动部署成功；[main 流水线 3339](http://47.120.4.95:7188/xgjy/repo-guard-acceptance-back/-/pipelines/3339) 质量、构建、部署全部成功 |

前端 4 项真实测试通过；后端 Java 检查、5 项测试及构建通过。前端不适用 Java 检查，后端不适用 Vue、样式及浏览器检查；不将这些跳过计为通过。

前端 [失败流水线 3337](http://47.120.4.95:7188/xgjy/repo-guard-acceptance/-/pipelines/3337) 的健康检查暴露多层代理重复版本响应头；已在包内修复并回归，重新打包后 3338 通过。失败作业仍返回 1，并有开始及失败通知，首次部署没有旧版本时保持维护状态。

## 部署与数据恢复

- 后端运行 MySQL 8.4、本地上传目录、独立 Redis 7.4；H2 仅用于隔离单元测试。
- 同一个安装产物在服务器 Docker 环境完成手动 `ops deploy`，包含质量检查、构建、镜像绑定、快照、切换及健康检查。
- 实际创建 MySQL 任务、上传文件、Redis 键值，记录发布前及发布后两组测试数据。
- 手动回滚：发布前数据保留，发布后数据在 MySQL、上传目录和 Redis 中均不存在。
- 再次回滚以切回较新版本：三类发布后数据全部恢复。随后仅删除记录的测试任务、文件和 Redis 键。
- 手动部署、回滚及再次切换，每次均有两条飞书平台发送成功回执。
- 公网 `http://47.120.4.95:18080/api/health` 已返回 200，版本为最终后端提交；不将此健康接口验证表述为全业务功能验收。
- 原 H2 任务列表为空；停服后完成冷备份 `/srv/repo-guard-acceptance/legacy-backup/h2-before-blue-green.tar.gz`，保留旧容器和原数据卷。

## 通知验证及边界

复用 `ci.notification` 的渠道和开关，GitLab 与手动方式一致。项目配置的是飞书：真实流水线和手动操作均获平台确认；另外以明确标注的“通知验收演练，服务未受影响”发送开始、成功、失败后回切、恢复失败四种消息，四次平台响应全部成功。

企业微信及双渠道独立失败处理通过自动测试，未配置企业微信真实机器人，因此未声称真实企业微信发送通过。恢复失败消息使用状态机自动测试及真实机器人演练验证；没有人为破坏在线 MySQL／上传／Redis 来制造恢复失败。通知成功表示平台确认，不等同于用户已阅读。

当前范围不包含合同强制放行、全业务接口验收、定时任务、外部消息消费者或跨仓库原子发布。备份保存在项目专用卷，不是异机灾备。前端功能分支已部署，尚未合入其 `main`；后端按用户要求合入 `main`。

补充验证边界：正式前端入口已验证健康与任务列表读取；额外的经前端入口新增任务／文件／Redis 读写复核未获执行许可，没有执行。三类数据的真实写入、读取、回滚和清理是在后端专用入口完成。

本机验证日志：`test/.tmp/deployment-clean-full-tests.log`、`test/.tmp/deployment-process-regression.log`、`test/.tmp/deployment-notification-boundary-tests.log`；服务器手动记录位于 `/srv/repo-guard-acceptance/build/manual-deploy.log`、`manual-rollback.log`、`manual-forward.log` 和 `notification-drill.log`。这些日志不包含于 npm 安装产物。
